const CrowBus = ({props}) => {
    return (<div className="CrowBus"></div>);
}

const CrowBusAndText = ({props}) => {
    return (<div className="CrowBusAndText"></div>);
}

const CrowBusAndDriver = ({props}) => {
    return (<div className="CrowBusAndDriver"></div>);
}

const Crow = ({props}) => {
    return (<div className="Crow"></div>);
}

const Bus = ({props}) => {
    return (<div className="Bus"></div>);
}

export { CrowBus, CrowBusAndText, CrowBusAndDriver, Crow, Bus }